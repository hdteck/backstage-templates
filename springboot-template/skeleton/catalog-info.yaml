apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Servicio Spring Boot generado desde Backstage
spec:
  type: service
  lifecycle: experimental
  owner: user:guest