apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Servicio Spring Boot
  tags:
    - java
    - springboot
spec:
  type: service
  owner: user:guest
  lifecycle: experimental